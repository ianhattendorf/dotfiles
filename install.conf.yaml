- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: ['~']

- link:
    ~/.gemrc: .gemrc
    ~/.gitconfig: .gitconfig
    ~/.gitignore: .gitignore
    ~/.irbrc: .irbrc
    ~/.nethackrc: .nethackrc
    ~/.vimrc: .vimrc
